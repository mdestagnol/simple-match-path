{
  "name": "simple-match-path",
  "version": "1.0.1",
  "description": "simple-match-path checks whether a specified URL (ex. \"/user/2\") match with a path (ex. \"/user/:id\"), and tells how it matched.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mdestagnol/simple-match-path"
  },
  "author": "Martin Destagnol",
  "license": "MIT",
  "dependencies": {
    "path-to-regexp": "^3.0.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "@types/path-to-regexp": "^1.7.0",
    "jest": "^24.8.0",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.3"
  }
}
